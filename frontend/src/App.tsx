// Live Arena UI scaffold (TODO.md Day 6).
// Will render: streaming chess board + move feed, betting panel (YES/NO pools,
// connect wallet, place bet, claim), and the trust strip (commit hash → revealed
// seed → "Verified by 0G Compute" badge).
export function App() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>Turing Pits</h1>
      <p>Live Arena scaffold. Board, betting panel, and trust strip land on Day 6.</p>
    </main>
  );
}

{
  "name": "turingpits",
  "version": "0.0.0",
  "private": true,
  "description": "Turing Pits — gamified AI Mafia prediction market on the 0G network",
  "workspaces": [
    "engine",
    "players",
    "contracts",
    "storage",
    "server",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20"
  }
}
